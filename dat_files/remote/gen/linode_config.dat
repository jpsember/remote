class {

  // Parameters for configuring a Linode

  string root_password;
  string access_token;

  // "A list of public SSH keys that will be automatically appended to the
  // root user’s ~/.ssh/authorized_keys file when deploying from an Image."
  //
  *string authorized_keys;
}
